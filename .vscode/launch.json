{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run app in external terminal",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/app",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "externalConsole": true, 
            "MIMode": "lldb",
            "preLaunchTask": "build app",

        }
    ]
}
